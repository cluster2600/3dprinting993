# PhysicsNeMo 2.2.0 requires torch>=2.10.0 and torchvision>=0.25.0a0.
# Keep its solver environment on the same explicit CUDA 12.9 wheel family as
# the Blackwell-capable local vLLM runtime; pip must never replace these pins.
torch==2.10.0
torchvision==0.25.0
